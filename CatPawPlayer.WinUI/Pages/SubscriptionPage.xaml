<Page
    x:Class="CatPawPlayer.WinUI.Pages.SubscriptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:CatPawPlayer.WinUI.Models">

    <ScrollViewer>
        <StackPanel Padding="24,16,24,24" Spacing="20">

            <!-- Header -->
            <StackPanel Spacing="4">
                <TextBlock Text="订阅管理" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="管理您的猫源/TVBox 订阅地址，切换后自动记忆并在下次启动时恢复。"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Add Subscription -->
            <StackPanel Spacing="8">
                <TextBlock Text="添加新订阅源" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SubUrlBox"
                             Grid.Column="0"
                             PlaceholderText="输入 TVBox / CatPaw 格式的订阅 URL (.json / .js.md5)..."
                             VerticalContentAlignment="Center"/>
                    <Button x:Name="AddSubBtn"
                            Grid.Column="1"
                            Content="保存并切换"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="AddSubBtn_Click"/>
                </Grid>

                <!-- Status InfoBar -->
                <InfoBar x:Name="StatusBar"
                         IsOpen="False"
                         IsClosable="True"/>
            </StackPanel>

            <MenuFlyoutSeparator/>

            <!-- Auto-Update Explanation Card -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="14,10">
                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE789;" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="🔄 智能更新机制：每次启动软件时，均会自动联网拉取激活源的最新站点与动态爬虫；您也可以在下方随时手动点击刷新以立即同步。"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Saved Subscriptions -->
            <StackPanel Spacing="8">
                <Grid>
                    <TextBlock Text="已保存的订阅源" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                        <Button x:Name="RefreshAllBtn"
                                Content="🔄 刷新全部订阅"
                                Style="{StaticResource SubtleButtonStyle}"
                                Padding="8,4"
                                Click="RefreshAllBtn_Click"/>
                        <TextBlock x:Name="SubCountText"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </Grid>

                <ListView x:Name="SubListView"
                          SelectionMode="None"
                          IsItemClickEnabled="True"
                          ItemClick="SubListView_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4,8" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Name & URL -->
                                <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CleanName}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Url}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding SiteCountText}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                </StackPanel>

                                <!-- Refresh Button -->
                                <Button Grid.Column="1"
                                        Tag="{Binding Id}"
                                        Click="RefreshSub_Click"
                                        Style="{StaticResource SubtleButtonStyle}"
                                        ToolTipService.ToolTip="重新拉取此订阅">
                                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                </Button>

                                <!-- Delete Button -->
                                <Button Grid.Column="2"
                                        Tag="{Binding Id}"
                                        Click="DeleteSub_Click"
                                        Style="{StaticResource SubtleButtonStyle}"
                                        ToolTipService.ToolTip="删除此订阅源">
                                    <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <MenuFlyoutSeparator/>

            <!-- Current Sites List -->
            <StackPanel x:Name="SitesPanel" Spacing="8">
                <TextBlock x:Name="SitesHeader" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <ListView x:Name="SitesListView"
                          MaxHeight="400"
                          SelectionMode="None"
                          IsItemClickEnabled="True"
                          ItemClick="SitesListView_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4,6" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding CleanName}"
                                           Style="{StaticResource BodyTextBlockStyle}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="1" Text="{Binding Api}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           FontFamily="Consolas"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
